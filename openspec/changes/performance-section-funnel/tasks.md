# Tasks

- [x] 1. `get_funnel()`: distinct-session counting, `reached`/`completed`/`reached_pct`/`dropped`/`dropped_pct`
- [x] 2. Tests: refresh counts once; percentages vs starts; no negative drop; markup scenario
- [x] 3. Redraw `analytics_performance.html` Section Funnel as step columns; drop Chart.js funnel
- [x] 4. CSS for `.perf-funnel*` in the analytics stylesheet; collectstatic
- [x] 5. Browser check at desktop and ~360px
