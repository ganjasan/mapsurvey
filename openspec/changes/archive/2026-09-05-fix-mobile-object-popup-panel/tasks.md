## 1. Fix

- [x] 1.1 `hidePanelForPopup` / `reshowPanel` in `layer_objects_block.html`, wired into `openObject` and the current popup's `popupclose`
- [x] 1.2 Playwright pass at 390×844 on the dev stand: row tap → panel hidden, popup in viewport; close → panel back
- [x] 1.3 Layers control with a legend auto-expands on desktop only; on a phone it covered the popup (same pass)
