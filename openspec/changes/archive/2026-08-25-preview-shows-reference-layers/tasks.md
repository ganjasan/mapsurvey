# Tasks: preview-shows-reference-layers

- [x] 1.1 `editor_section_preview`: pass `map_layers` (shared helper from `views`) and
      `hidden_layers_json` for the previewed section.
- [x] 1.2 Tests: preview carries layer config and the section's hidden list; nothing when
      the kill switch is off.
- [x] 1.3 Full `./run_tests.sh survey`; browser check of the editor preview.
- [x] 1.4 PR referencing this change.
