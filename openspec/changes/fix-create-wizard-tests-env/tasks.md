# Tasks

- [x] 1. Add class-level `@override_settings(AI_PROVIDER='anthropic', ANTHROPIC_API_KEY='sk-test')` to `CreateSurveyWizardTest`
- [x] 2. Run `CreateSurveyWizardTest` on a checkout whose `.env` has no AI keys — all 5 tests pass
- [ ] 3. Mark backlog #148 resolved (root cause: env-dependent tests, wizard itself healthy)
